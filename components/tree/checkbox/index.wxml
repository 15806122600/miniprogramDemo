<view class="wrapper">
  <view class="round {{ checked ? 'checked' : '' }}">
    <!-- <view wx:if="{{ checked }}" class="icon-check iconfont-ws icon-ws-hook" /> -->
    <view wx:if="{{ checked }}" class="icon-check icon icon-hook" />
  </view>
</view>