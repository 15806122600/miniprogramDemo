<!-- <view class="container">
   <v-language />
  <text>{{content.text1}}</text>
  <text>{{content.text2}}</text>
  <text>{{content.text3}}</text> 
 <button bindtap="getUserProfile">获取用户信息</button>
  <view style="height: 10px;"></view>

  <form bindsubmit="updateUserInfo">
    <view class="cu-form-group">
      <button class="avatar-wrapper" open-type="chooseAvatar" bind:chooseavatar="onChooseAvatar">
        <image class="avatar" src="{{avatarUrl}}"></image>
      </button>
      <view class="title">昵称：</view>
      <input placeholder="请输入昵称" type="nickname" name="nickname" maxlength="32"></input>
    </view>
    <view class="up-bt">
      <button class="save-bt cu-btn block bg-blue margin-tb-sm lg white" form-type="submit" role="button" aria-disabled="false">更新资料</button>
    </view>
  </form>
  <button class="save-bt cu-btn block bg-blue margin-tb-sm lg white" bindtap="goEcharts" role="button" aria-disabled="false">图表横屏展示</button>
  <van-uploader file-list="{{ fileList }}" bind:after-read="afterRead" multiple="true"/>
  <block wx:if="{{ImageList.length>0}}">
    <view class="cu-list menu sm-border card-menu">
      <view class="cu-item">
        <view class="content top">
          <view class="grid flex-sub col-6 grid-square">
            <view wx:for="{{ImageList}}" wx:key="index" class="bg-img">
              <van-image fit="fill" width="45" height="45" src="{{item.url}}" data-url="{{item.url}}" bind:click="onPreviewImage" />
            </view>
          </view>
        </view>
      </view>
    </view>
  </block>
</view> -->